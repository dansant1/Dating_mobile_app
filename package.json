{
  "name": "chat2",
  "private": true,
  "scripts": {
    "start": "meteor --settings settings.json",
    "deploy": "DEPLOY_HOSTNAME=galaxy.meteor.com meteor deploy v1.sincompromiso.pe  --settings settings.json"
  },
  "dependencies": {
    "babel-runtime": "^6.20.0",
    "bcrypt": "^5.0.0",
    "call-number": "^1.0.1",
    "is-property": "^1.0.2",
    "meteor-node-stubs": "~0.2.0"
  }
}
